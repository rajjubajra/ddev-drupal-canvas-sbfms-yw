/**
 * @file
 * YW Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.ywTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
